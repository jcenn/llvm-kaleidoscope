extern printf(pattern: string, val: i32);

fn main(){
    let i: i32 = 0;
    for i < 5 {
        if i % 2 == 0 {
            printf("number %d is even", i);
        }else {
            printf("number %d is odd", i);
        }
        i = i + 1;
    }
    return 0;
}

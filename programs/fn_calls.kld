// EXPECT 8

fn add(x, y) -> i32 {
    return x + y;
}

fn sub(x, y) -> i32{
    return x - y;
}

// Functions without a type specifier are assumed to return void
fn test() {
    add(1, 1);
    return;
}

fn main(){
    test();
    sub(1, 1);
    return add(1, 3) + sub(5, 1);
}

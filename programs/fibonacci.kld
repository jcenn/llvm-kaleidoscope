// compute the n-th value of thea fibonacci sequence
fn fib(n) -> i32 {
    // n == 1
    if n - 1 {
        return 1;
    }
    // n == 2
    if n - 2{
        return 1;
    }

    return fib(n-1) + fib(n-2);
}

fn main() {
    return fib(12);
}

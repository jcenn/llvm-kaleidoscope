// EXPECTS 15

// sums all values from 0 to x
fn sum(x: i32) -> i32 {
    if x == 1 {
        return x;
    }
    return x + sum(x - 1);
}

fn main() {
    // 1 + 2 + 3 + 4 + 5 => 15
    return sum(5);
}
